# Peer resolution: install missing peers, but do not fail the install on a
# mismatch (a few React 19 / Next 16 peers upstream are still declared for 18/15).
auto-install-peers=true
strict-peer-dependencies=false
